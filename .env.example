# Copy this file to .env and fill in your Square access token
# (This is only needed if you want to run the script locally —
# for the automated GitHub version, the token is stored as a GitHub Secret)

SQUARE_ACCESS_TOKEN=your-square-access-token-here
